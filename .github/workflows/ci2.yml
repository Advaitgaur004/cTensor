name: Build and Test 2

on:
  push:
    branches: [ "main", "ci" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-linux:
    name: linux (${{ matrix.c_compiler }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        c_compiler: [gcc, clang]
        cpp_compiler: [g++, clang++]
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y build-essential cmake ${{ matrix.c_compiler }}
    - name: Build project
      run: |
        mkdir -p build
        cd build
        cmake .. -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        cmake --build .
    - name: Run example
      run: |
        cd build
        echo "=== ${{ matrix.c_compiler }} results ===" > ../results.txt
        echo "date: $(date)" >> ../results.txt
        echo "platform: linux" >> ../results.txt
        echo "compiler: ${{ matrix.c_compiler }}" >> ../results.txt
        ./cten_exe >> ../results.txt 2>&1 || echo "program had issues but whatever"
    - uses: actions/upload-artifact@v4
      with:
        name: linux-${{ matrix.c_compiler }}
        path: results.txt

  build-macos:
    name: Build on macOS
    runs-on: macos-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Install dependencies
      run: brew install cmake
    - name: Build project
      run: |
        mkdir -p build
        cd build
        cmake ..
        cmake --build .
    - name: Run example
      run: |
        cd build
        echo "=== macos results ===" > ../results.txt
        echo "date: $(date)" >> ../results.txt
        echo "platform: macos" >> ../results.txt
        echo "compiler: clang" >> ../results.txt
        ./cten_exe >> ../results.txt 2>&1 || echo "program had issues but whatever"
    - uses: actions/upload-artifact@v4
      with:
        name: macos
        path: results.txt

  build-windows:
    name: windows (msvc)
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: setup msvc
      uses: ilammy/msvc-dev-cmd@v1
    - name: Build project
      shell: pwsh
      run: |
        mkdir -p build
        cd build
        cmake ..
        cmake --build .
    - name: Run example
      shell: pwsh
      run: |
        cd build
        echo "=== windows results ===" > ../results.txt
        echo "date: $(Get-Date)" >> ../results.txt
        echo "platform: windows" >> ../results.txt
        echo "compiler: msvc" >> ../results.txt
        ./Debug/cten_exe.exe >> ../results.txt 2>&1 || echo "exe failed but whatever"
    - uses: actions/upload-artifact@v4
      with:
        name: windows
        path: results.txt
        
  pytorch-comparison:
    runs-on: ubuntu-latest
    needs: [build-linux, build-macos, build-windows]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    - name: install pytorch
      run: pip install torch scikit-learn numpy
    - name: get all results
      uses: actions/download-artifact@v4
      with:
        pattern: "*"
        merge-multiple: true
    - name: run pytorch version
      run: python .github/scripts/pytorch_comparison.py > pytorch_results.txt
    - name: compare everything
      run: |
        python .github/scripts/compare_results.py
        echo "=== comparison done ==="
        cat comparison_report.txt || echo "no analysis file found"
    - uses: actions/upload-artifact@v4
      with:
        name: all-comparisons
        path: |
          *.txt